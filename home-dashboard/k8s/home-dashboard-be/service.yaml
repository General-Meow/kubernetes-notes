apiVersion: v1
kind: Service
metadata:
  labels:
    app: home-dashboard-be-service
  name: home-dashboard-be-service
spec:
  type: ClusterIP
  selector:
    app: home-dashboard-be
  ports:
    - port: 3001
      protocol: TCP
      targetPort: 3001
